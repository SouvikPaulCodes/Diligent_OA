Create a file named ingestToDB.js

Tasks:
- Use sqlite3 package
- Create a database named ecommerce.db
- Enable foreign keys (`PRAGMA foreign_keys = ON`)
- Create tables matching the CSV files:

users(id INTEGER PRIMARY KEY, name TEXT, email TEXT, created_at TEXT)
products(id INTEGER PRIMARY KEY, name TEXT, category TEXT, price REAL)
orders(id INTEGER PRIMARY KEY, user_id INTEGER, order_date TEXT, total_amount REAL,
       FOREIGN KEY(user_id) REFERENCES users(id))
order_items(id INTEGER PRIMARY KEY, order_id INTEGER, product_id INTEGER,
            quantity INTEGER, item_price REAL,
            FOREIGN KEY(order_id) REFERENCES orders(id),
            FOREIGN KEY(product_id) REFERENCES products(id))
payments(id INTEGER PRIMARY KEY, order_id INTEGER, payment_method TEXT,
         payment_status TEXT, payment_date TEXT,
         FOREIGN KEY(order_id) REFERENCES orders(id))

- Write a helper function loadCSV(path) that reads CSV data using fs.readFileSync + split
- Insert all rows into the correct tables
- Log number of rows inserted per table
